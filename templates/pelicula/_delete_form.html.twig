<form method="post"
      action="{{ path('app_pelicula_delete', {'id': pelicula.id}) }}"
      onsubmit="return confirm('¿Seguro que quieres borrar esta película?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pelicula.id) }}">

    <button class="btn btn-danger btn-sm mt-2">🗑 Borrar</button>
</form>
